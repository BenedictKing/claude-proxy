{
  "name": "claude-api-proxy",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun run dev-runner.ts",
    "dev:watch": "bun --watch run server.ts",
    "dev:hot": "bun --watch --hot run server.ts",
    "config": "bun run config-cli.ts"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "@types/express": "^5.0.3",
    "chokidar": "^3.6.0",
    "dotenv": "^17.2.2",
    "express": "^4.19.2"
  }
}
